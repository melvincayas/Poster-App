<%- layout('./templates/boilerplate') -%>
<div class="row">
    <div class="col-md-6 offset-md-3">
        <% if (username) { %> 
        <section>
            <h1>What's happening?</h1>
            <form class="needs-validation" action="/posts" method="POST" novalidate>
                <textarea class="mt-4 form-control" name="body" placeholder="Everyone's waiting to hear from you!" required></textarea>
            <div class="text-end">
            <button class="mt-2 my-4 btn-sm btn-primary">Submit</button>
            </div>
            </form>
        </section>
        <% } %> 

        <section>
        <h1 class="mb-4">Latest Posts</h1>
        <% for (let post of posts ) { %>
            <article class="mb-4 card">
                <h5 class="card-header">
                    <div class="d-flex justify-content-between">
                    <a href="/user/<%= post.user.username %>"><%= post.user.username %></a>
                    <a href="/posts/show/<%= post._id %>"><i class="fas fa-ellipsis-h"></i></a>
                    </div>
                </h5> 
                <div class="card-body">
                    <p class="card-text"><%= post.body %></p>
                </div>
                <div class="card-footer text-muted">
                    <a href="/posts/show/<%= post._id %>"><%= post.comments.length %> <%= post.comments.length === 1 ? 'Comment' : 'Comments' %></a> / 
                    <%= post.date %>
                </div>
            </article>
        <% } %>
        </section>
    </div>
</div>